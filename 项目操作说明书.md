# 论文智能聚类与推荐系统操作说明书

## 1. 项目简介

本项目是一个面向 arXiv 计算机相关论文的数据挖掘系统，核心目标是把大量论文元数据转化为可检索、可推荐、可聚类、可视化的分析平台。系统使用本地 arXiv JSON Lines 快照作为初始数据源，使用 SPECTER2 模型为论文标题和摘要生成语义向量，并通过 FastAPI 后端和 Vue 前端提供交互式论文检索、推荐和聚类展示功能。

项目适合课程设计、数据挖掘实验和论文推荐系统演示。课程答辩时可以重点展示：大规模论文数据导入、embedding 生成、相似论文推荐、聚类可视化、手动增量更新和前后端完整交互流程。

主要技术栈：

| 层次 | 技术 |
| --- | --- |
| 后端服务 | Python、FastAPI、SQLite |
| 机器学习 | SPECTER2、PyTorch、Transformers、Adapters |
| 向量处理 | NumPy |
| 推荐算法 | 余弦相似度 |
| 聚类降维 | KMeans、PCA|
| 前端界面 | Vue 3、Vite、Axios、ECharts |

## 2. 功能总览

| 功能 | 说明 | 主要入口 |
| --- | --- | --- |
| 本地快照导入 | 从本地 arXiv JSONL 文件全量重建数据库 | `python -m backend.app.cli import-arxiv-snapshot` |
| 数据库初始化 | 创建 SQLite 表结构 | `python -m backend.app.cli init-db` |
| embedding 生成 | 使用 SPECTER2 为未生成向量的论文生成语义向量 | `python -m backend.app.cli build-embeddings` |
| 向量存储 | 保存论文 ID 顺序和 embedding 矩阵 | `data/paper_ids.json`、`data/embeddings.npy` |
| 论文搜索 | 用 SQLite FTS5 倒排索引检索标题和摘要，并按 BM25 相关度排序 | 前端搜索框、`GET /api/papers` |
| 分类筛选 | 按 arXiv 分类筛选论文 | 前端分类下拉框、`GET /api/categories` |
| 论文详情 | 查看标题、作者、分类、摘要等信息 | 前端详情面板、`GET /api/papers/{paper_id}` |
| 相似论文推荐 | 根据选中论文的 embedding 推荐相似论文 | `GET /api/papers/{paper_id}/recommendations` |
| 研究兴趣推荐 | 根据用户输入文本生成查询向量并推荐论文 | `POST /api/recommendations/query` |
| 聚类分析 | 对论文 embedding 执行 KMeans 聚类 | `python -m backend.app.cli cluster --clusters 12` |
| 二维可视化 | 展示聚类后的二维散点图 | 前端 ECharts、`GET /api/clusters/plot` |
| 数据统计 | 展示论文总数、向量数量、聚类数量、轮廓系数 | 前端统计卡片、`GET /api/stats` |
| 手动增量更新 | 从 arXiv API 抓取上次更新至今的新论文，入库并生成 embedding | 前端“更新数据”、`python -m backend.app.cli sync-arxiv-incremental` |
| 增量 raw 记录 | 保存手动更新抓取到的新论文元数据 | `data/raw/arxiv_incremental.jsonl` |
| 前端服务 | 提供浏览器交互界面 | `scripts/run_frontend.ps1` |
| 后端服务 | 提供 API、同步、推荐和统计能力 | `scripts/run_backend.ps1` |
| 测试验证 | 验证后端逻辑和前端构建 | `python -m pytest`、`npm run build` |

## 3. 数据说明

### 3.1 初始数据集

当前初始数据集位于：

```text
data/raw/arxiv_cs_ai_8cats_until_20260530.json
```

该文件是 JSON Lines 格式，每一行代表一篇论文。系统按去掉版本号后的 arXiv ID 去重，例如 `2401.00001v2` 会按 `2401.00001` 入库。

数据统计文件位于：

```text
data/raw/arxiv_cs_ai_8cats_until_20260530_stats.json
```

当前数据集关键信息：

| 项目 | 数值 |
| --- | --- |
| 截止时间 | 2026-05-30 23:59 |
| 总论文数 | 615819 |
| 文件格式 | JSON Lines，每行一篇论文 |
| 去重规则 | 按去掉版本号后的 arXiv ID 去重 |

### 3.2 支持的 arXiv 分类

系统当前关注 8 个 arXiv CS 分区：

| 分类 | 统计数量 |
| --- | ---: |
| `cs.AI` | 181174 |
| `cs.CL` | 110492 |
| `cs.CV` | 194072 |
| `cs.DB` | 11740 |
| `cs.DS` | 28842 |
| `cs.IR` | 25462 |
| `cs.LG` | 269800 |
| `cs.SE` | 26945 |

一篇论文可能同时属于多个分类，因此分类统计数量相加会大于总论文数。

### 3.3 运行时数据文件

| 文件 | 作用 |
| --- | --- |
| `data/app.db` | SQLite 数据库，保存论文元数据、embedding 状态、聚类编号和二维坐标 |
| `data/embeddings.npy` | NumPy 格式的 embedding 矩阵 |
| `data/paper_ids.json` | 与 embedding 矩阵行顺序对应的论文 ID 列表 |
| `data/raw/arxiv_incremental.jsonl` | 手动增量更新抓取到的论文记录 |
| `logs/arxiv_incremental.log` | 手动增量更新执行日志 |
| `data/model_cache/` | Hugging Face/SPECTER2 本地模型缓存 |

## 4. 环境准备

### 4.1 创建 Conda 环境

在项目根目录执行：

```powershell
.\scripts\create_env.ps1
```

该脚本会：

1. 创建或复用 `paper-embed-rec` Conda 环境。
2. 根据 `environment.yml` 安装 Python 和基础依赖。
3. 安装 CUDA 12.8 对应的 GPU 版 PyTorch。
4. 提示模型下载使用 `HF_ENDPOINT=https://hf-mirror.com`。

创建完成后，手动激活环境：

```powershell
conda activate paper-embed-rec
```

### 4.2 模型缓存说明

系统使用 SPECTER2 生成论文语义向量，模型缓存目录为：

```text
data/model_cache/
```

当前后端会优先检查本地必要模型文件。如果文件已经存在，就直接从本地加载；只有缺失或文件大小为 0 时才会尝试从 Hugging Face 镜像站补下载。

主要模型目录：

| 目录 | 作用 |
| --- | --- |
| `data/model_cache/allenai__specter2_base` | SPECTER2 base model 和 tokenizer |
| `data/model_cache/allenai__specter2` | SPECTER2 adapter |

## 5. 数据初始化流程

第一次运行系统，推荐按下面顺序初始化。

### 5.1 初始化数据库

```powershell
python -m backend.app.cli init-db
```

该命令会创建 SQLite 表结构。如果数据库已存在，重复执行是安全的。

### 5.2 从本地快照全量导入论文

```powershell
python -m backend.app.cli import-arxiv-snapshot
```

该命令默认读取：

```text
data/raw/arxiv_cs_ai_8cats_until_20260530.json
```

注意：这是全量重建命令，会清空旧数据库论文数据、旧 embedding 文件和旧论文 ID 文件，再重新导入本地快照。它不会自动生成全部 embedding。

如果要指定其他 JSONL 快照文件：

```powershell
python -m backend.app.cli import-arxiv-snapshot --source D:\path\to\snapshot.jsonl
```

### 5.3 生成论文 embedding

```powershell
python -m backend.app.cli build-embeddings --batch-size 16
```

该命令会读取数据库中尚未生成 embedding 的论文，把论文标题和摘要拼接为：

```text
title [SEP] abstract
```

然后使用 SPECTER2 编码，结果保存到：

```text
data/embeddings.npy
data/paper_ids.json
```

如果本机 GPU 可用，PyTorch 会优先使用 CUDA；如果不可用，则回退到 CPU。

### 5.4 执行聚类和二维降维

```powershell
python -m backend.app.cli cluster --clusters 12
```

该命令会：

1. 读取 `data/embeddings.npy` 和 `data/paper_ids.json`。
2. 使用 KMeans 聚类。
3. 优先使用 PyTorch CUDA 在 GPU 上执行 PCA 降到二维；如果 GPU 不可用或显存不足，则回退 CPU PCA。
4. 把 `cluster_id`、`x`、`y` 写回 SQLite。
5. 计算可用情况下的轮廓系数。

完成后，前端聚类散点图才会显示点位。

## 6. 启动系统

### 6.1 启动后端服务

在项目根目录执行：

```powershell
.\scripts\run_backend.ps1
```

该脚本会启动 FastAPI：

```text
http://127.0.0.1:8000
```

后端使用 `--reload` 开发模式，代码变化后会自动重载。

### 6.2 启动前端服务

另开一个 PowerShell 窗口，在项目根目录执行：

```powershell
.\scripts\run_frontend.ps1
```

该脚本会进入 `frontend` 目录，安装或更新 npm 依赖，然后启动 Vite 开发服务：

```text
http://127.0.0.1:5173
```

Vite 已配置 `/api` 代理到：

```text
http://127.0.0.1:8000
```

因此前端代码不需要硬编码后端端口。

## 7. 前端使用说明

打开浏览器访问：

```text
http://127.0.0.1:5173
```

页面主要区域如下：

### 7.1 顶部操作区

| 控件 | 作用 |
| --- | --- |
| 刷新数据 | 重新加载论文列表、统计信息和聚类图 |
| 更新数据 | 按上次成功更新时间到当前时间手动抓取新论文，入库并生成 embedding |

### 7.2 统计卡片

页面顶部展示：

| 指标 | 说明 |
| --- | --- |
| 论文总数 | 数据库中论文数量 |
| 已生成向量 | `embedding_ready = 1` 的论文数量 |
| 聚类数量 | 已写入聚类结果的聚类数 |
| 轮廓系数 | 聚类质量参考指标，未计算时显示“未计算” |

### 7.3 搜索和筛选

左侧搜索面板支持：

1. 输入关键词搜索标题或摘要，后端使用 FTS5 倒排索引和 BM25 相关度排序。
2. 选择 arXiv 分类筛选论文。
3. 输入研究兴趣文本，点击“按研究兴趣推荐”。

分类选项来自后端 `/api/categories`，不是前端硬编码。以后后端分类范围变化时，前端会自动跟随。

### 7.4 论文列表

中间区域展示论文列表，每条论文包含：

1. 标题。
2. arXiv 分类。
3. 摘要预览。

点击某篇论文后，右侧会加载该论文详情。

### 7.5 论文详情

右侧详情面板展示：

1. 论文标题。
2. 作者。
3. 分类。
4. 摘要。
5. “推荐相似论文”按钮。

点击“推荐相似论文”后，系统会以当前论文的 embedding 为查询向量，返回最相似论文，并排除当前论文自身。

### 7.6 研究兴趣推荐

在左侧文本框输入研究兴趣，例如：

```text
large language model retrieval
```

点击“按研究兴趣推荐”后，后端会把输入文本编码成查询向量，再和论文 embedding 计算余弦相似度，返回 Top-K 推荐结果。

### 7.7 聚类散点图

页面底部是二维聚类可视化图：

1. 每个点代表一篇论文。
2. 点的颜色由聚类编号决定。
3. 鼠标悬停可查看论文标题和聚类编号。
4. 点击散点可联动右侧论文详情面板。

如果聚类图为空，通常表示还没有执行：

```powershell
python -m backend.app.cli cluster --clusters 12
```

## 8. 后端 API 说明

| 方法 | 路径 | 作用 |
| --- | --- | --- |
| `GET` | `/api/papers` | 分页查询论文，关键词搜索使用 FTS5 + BM25，并支持分类、聚类筛选 |
| `GET` | `/api/papers/{paper_id}` | 查询单篇论文详情 |
| `GET` | `/api/papers/{paper_id}/recommendations` | 根据指定论文推荐相似论文 |
| `POST` | `/api/recommendations/query` | 根据用户输入文本推荐论文 |
| `GET` | `/api/clusters/plot` | 返回二维聚类散点图数据 |
| `GET` | `/api/stats` | 返回论文总数、embedding 数、聚类数、轮廓系数 |
| `GET` | `/api/categories` | 返回当前支持的 arXiv 分类及数量 |
| `POST` | `/api/sync/arxiv-incremental/start` | 启动手动 arXiv 增量更新后台任务 |
| `GET` | `/api/sync/arxiv-incremental/status/{job_id}` | 读取手动增量更新任务进度 |
| `GET` | `/api/sync/status` | 读取最近手动增量更新日志 |

### 8.1 论文查询接口

示例：

```text
GET /api/papers?query=transformer&category=cs.CL&page=1&page_size=20
```

当传入 `query` 时，后端会先把用户输入规范化成安全的全文检索词，再查询 `papers_fts`
倒排索引。排序使用 SQLite `bm25()`，标题字段权重大于摘要字段，因此标题直接命中的论文
通常会排在只在摘要中命中的论文前面。

返回内容包含：

1. `items`：论文列表。
2. `total`：总命中数量。
3. `page`：当前页。
4. `page_size`：每页数量。

### 8.2 文本推荐接口

请求：

```http
POST /api/recommendations/query
```

请求体：

```json
{
  "query": "large language model retrieval",
  "top_k": 10
}
```

返回内容为推荐论文列表，每篇论文带有相似度分数。

## 9. 命令行工具说明

命令行入口为：

```powershell
python -m backend.app.cli <command>
```

支持命令如下：

| 命令 | 作用 | 常用场景 |
| --- | --- | --- |
| `init-db` | 初始化 SQLite 数据库 | 第一次运行或重建前 |
| `import-arxiv-snapshot` | 从本地 arXiv JSONL 快照全量重建数据库 | 初始导入或更换数据集 |
| `build-embeddings` | 为未生成 embedding 的论文生成向量 | 导入新论文后 |
| `cluster` | 执行 KMeans 聚类和二维降维 | 生成聚类可视化前 |
| `sync-arxiv-incremental` | 按上次更新时间手动增量更新 arXiv 新论文并生成向量 | 手动扩充数据集 |

推荐完整流程：

```powershell
python -m backend.app.cli init-db
python -m backend.app.cli import-arxiv-snapshot
python -m backend.app.cli build-embeddings --batch-size 16
python -m backend.app.cli cluster --clusters 12
```

手动增量维护流程：

```powershell
python -m backend.app.cli sync-arxiv-incremental
python -m backend.app.cli cluster --clusters 12
```

手动增量更新会为新增论文生成 embedding；如果新增论文较多，聚类建议手动重新执行。

## 10. PowerShell 脚本说明

项目 `scripts` 目录提供 4 个常用脚本。

| 脚本 | 功能 |
| --- | --- |
| `create_env.ps1` | 创建或复用 Conda 环境，并安装 GPU 版 PyTorch |
| `run_backend.ps1` | 启动 FastAPI 后端服务 |
| `run_frontend.ps1` | 安装前端依赖并启动 Vue/Vite 前端服务 |
| `sync_arxiv_incremental.ps1` | 手动执行 arXiv 增量更新并生成新增论文向量 |

### 10.1 create_env.ps1

用途：准备项目运行环境。

关键行为：

1. 环境名固定为 `paper-embed-rec`。
2. 如果环境已存在，就复用环境。
3. 如果环境不存在，就根据 `environment.yml` 创建。
4. 显式安装 CUDA 12.8 对应的 PyTorch。

### 10.2 run_backend.ps1

用途：启动后端。

实际执行：

```powershell
conda run -n paper-embed-rec uvicorn backend.app.main:app --host 127.0.0.1 --port 8000 --reload
```

### 10.3 run_frontend.ps1

用途：启动前端。

关键行为：

1. 进入 `frontend` 目录。
2. 使用指定 Node/npm 路径。
3. 使用 `https://registry.npmmirror.com` 安装依赖。
4. 执行 `npm run dev`。

### 10.4 sync_arxiv_incremental.ps1

用途：手动更新 arXiv 增量数据。

实际执行：

```powershell
conda run -n paper-embed-rec python -m backend.app.cli sync-arxiv-incremental
```

## 11. 手动增量更新机制

手动增量更新由 `backend/app/services/sync.py` 实现，流程如下：

1. 根据 8 个目标分类构造 arXiv API 查询。
2. 读取上次成功更新时间；首次运行从 `2026-05-30 23:59 UTC` 开始。
3. 拉取该时间到当前时间之间首次提交的论文元数据。
4. 将抓取记录追加到 `data/raw/arxiv_incremental.jsonl`。
5. 写入 SQLite，数据库按 arXiv ID 去重。
6. 只为本次窗口内缺失 embedding 的论文生成向量。
7. 成功后把本次窗口结束时间写入 `app_state`。
8. 把更新结果写入 `logs/arxiv_incremental.log`。

更新可以通过两种方式触发：

| 方式 | 操作 |
| --- | --- |
| 前端按钮 | 点击“更新数据” |
| 命令行 | `python -m backend.app.cli sync-arxiv-incremental` |

## 12. 测试与验证

### 12.1 后端测试

在项目根目录执行：

```powershell
python -m pytest -q
```

如果当前环境存在 pytest 缓存权限问题，可以执行：

```powershell
python -m pytest -q -p no:cacheprovider
```

测试覆盖：

1. 本地 arXiv 快照规范化和全量重建。
2. CLI 导入命令。
3. 手动 arXiv 增量更新和增量 JSONL 写入。
4. 分类 API。

### 12.2 前端构建

进入前端目录：

```powershell
cd frontend
npm run build
```

构建成功表示 Vue 代码、Vite 配置和依赖打包正常。

## 13. 常见问题

### 13.1 前端论文列表为空

可能原因：

1. 没有执行数据库初始化。
2. 没有执行 `import-arxiv-snapshot`。
3. 后端服务没有启动。
4. 前端代理无法连接 `http://127.0.0.1:8000`。

建议检查：

```powershell
python -m backend.app.cli init-db
python -m backend.app.cli import-arxiv-snapshot
.\scripts\run_backend.ps1
.\scripts\run_frontend.ps1
```

### 13.2 推荐接口提示尚未生成 embedding

说明数据库里有论文，但还没有向量文件。执行：

```powershell
python -m backend.app.cli build-embeddings --batch-size 16
```

### 13.3 聚类图为空

说明还没有聚类和二维坐标。执行：

```powershell
python -m backend.app.cli cluster --clusters 12
```

### 13.4 模型下载慢或失败

系统会设置：

```text
HF_ENDPOINT=https://hf-mirror.com
```

如果模型已经在 `data/model_cache/` 中完整存在，后端会直接从本地加载，不需要每次联网下载。

### 13.5 手动增量更新失败

可以查看：

```text
logs/arxiv_incremental.log
```

常见原因：

1. 网络无法访问 arXiv API。
2. Conda 环境未正确创建。
3. 模型依赖或本地模型文件缺失。
4. 数据库文件被其他进程占用。

### 13.6 重新导入快照后旧推荐结果不对

`import-arxiv-snapshot` 是全量重建命令，会删除旧 embedding 和论文 ID 顺序文件。重新导入后必须重新执行：

```powershell
python -m backend.app.cli build-embeddings --batch-size 16
python -m backend.app.cli cluster --clusters 12
```

## 14. 项目目录速查

| 路径 | 说明 |
| --- | --- |
| `backend/app/main.py` | FastAPI 应用和 API 路由 |
| `backend/app/cli.py` | 命令行入口 |
| `backend/app/config.py` | 路径、分类、模型和同步配置 |
| `backend/app/db.py` | SQLite 数据访问层 |
| `backend/app/datasets/local_arxiv.py` | 本地 arXiv 快照读取、清洗和全量导入 |
| `backend/app/datasets/arxiv_incremental.py` | arXiv API XML 解析和手动增量记录映射 |
| `backend/app/models/embedding.py` | SPECTER2 embedding 模型封装 |
| `backend/app/services/recommendation.py` | 相似论文推荐算法 |
| `backend/app/services/clustering.py` | KMeans 聚类和二维降维 |
| `backend/app/services/sync.py` | 手动增量更新 |
| `backend/app/services/vector_store.py` | embedding 文件读写 |
| `frontend/src/App.vue` | 前端主页面和交互逻辑 |
| `frontend/src/styles.css` | 前端样式 |
| `scripts/` | 环境、启动和手动增量更新脚本 |
| `tests/` | 后端测试 |
| `data/raw/` | 原始快照和手动增量数据 |
| `logs/` | 同步日志 |
